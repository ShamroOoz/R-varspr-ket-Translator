const SelectDropDown = ({ selectedLanguage }) => {
  return (
    <div className="select-drop-down">
      <input value={selectedLanguage} defaultValue />
    </div>
  );
};

export default SelectDropDown;
